#include <stdio.h>
#include<vector>
#include<edge.h>

findedgeloop(bool[n][n]) 
{
  struct bool checkLoop(int a, int b){   
  };
  int n;
  adj_matrix[n][n] = bool [n][n];
  for all edge 
  {
   if (checkLoop(i,j) ) 
   {
      adj_matrix[i][j] = false; 
   }
    else 
    {continue;}
    //checks if its an edge loop or not
  }
}

