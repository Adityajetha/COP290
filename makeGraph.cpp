#include<stdio.h>
#include<stdlib.h>

makeGraph()
{
  bool[][] adj_matrix;
  struct AdjListNode
  {
  vertex dest;
  struct AdjListNode* next;
  }
  struct Graph* createGraph(vertex V){}
  struct AddEdge(vertex src, vertex des, struct Graph* graph){}
  
  
}
