#include <stdio.h>
#include "vertex.h"

class vertex{}

public class two3{
             int num;
             bool valid,opn;
             vertex ver[] = ver[num];
             public static void main(){
             cin>>opn;
             for each vertex
             {
               xy();
               valid=valid && yz();
               valid=valid && xz();
             }
  if(opn)
    createedge();
  else
    edgelist();
             }
}
