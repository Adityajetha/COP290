#include<stdio.h>
#include<face.h>
#include <bits/stdc++.h>
public struct create_3D {

public : int num; ////num is the number of vertices
             public : bool[][] adj_matrix; ////adj_matrixis the adjacency matrix
             public : vertex ver[] = new vertex[num];
                        ///aray of vertices
             public : std::vector<face> face_list = new std::vector<face>;
                        
             public : int  n,num_vertex;
     
             public :bool checkFace(vector<face> face_list,bool[][] adj_matrix)
             
               ///checks if a face is real or not
             
             
             
                    }
